<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.finance.FinBudgetMapper">

    <resultMap type="FinBudget" id="FinBudgetResult">
        <result property="id"                column="id"                />
        <result property="userId"            column="user_id"           />
        <result property="categoryId"        column="category_id"       />
        <result property="categoryName"      column="category_name"     />
        <result property="year"              column="year"              />
        <result property="month"             column="month"             />
        <result property="amount"            column="amount"            />
        <result property="warningThreshold"  column="warning_threshold" />
        <result property="warned"            column="warned"            />
        <result property="delFlag"           column="del_flag"          />
        <result property="createBy"          column="create_by"         />
        <result property="createTime"        column="create_time"       />
        <result property="updateBy"          column="update_by"         />
        <result property="updateTime"        column="update_time"       />
        <result property="remark"            column="remark"            />
    </resultMap>

    <sql id="selectFinBudgetVo">
        select b.id, b.user_id, b.category_id, c.name as category_name, b.amount, b.year, b.month,
               b.warning_threshold, b.warned, b.del_flag, b.create_by, b.create_time, b.update_by, b.update_time, b.remark
        from fin_budgets b
        left join fin_categories c on b.category_id = c.id
    </sql>

    <!-- 查询预算列表 -->
    <select id="selectFinBudgetList" parameterType="FinBudget" resultMap="FinBudgetResult">
        <include refid="selectFinBudgetVo"/>
        <where>
            b.del_flag = '0'
            <if test="userId != null "> and b.user_id = #{userId}</if>
            <if test="categoryId != null "> and b.category_id = #{categoryId}</if>
            <if test="year != null "> and b.year = #{year}</if>
            <if test="month != null "> and b.month = #{month}</if>
            <if test="params.beginAmount != null and params.endAmount != null"> and b.amount between #{params.beginAmount} and #{params.endAmount}</if>
            <if test="warned != null "> and b.warned = #{warned}</if>
        </where>
        order by b.year desc, b.month desc
    </select>

    <!-- 查询预算详情 -->
    <select id="selectFinBudgetById" parameterType="Long" resultMap="FinBudgetResult">
        <include refid="selectFinBudgetVo"/>
        where b.id = #{id} and b.del_flag = '0'
    </select>

    <!-- 查询用户指定月份总预算 -->
    <select id="selectFinBudgetByUserAndMonth" resultMap="FinBudgetResult">
        <include refid="selectFinBudgetVo"/>
        where b.user_id = #{userId}
        and b.category_id is null
        and b.year = #{year}
        and b.month = #{month}
        and b.del_flag = '0'
    </select>

    <!-- 查询用户指定分类预算 -->
    <select id="selectFinBudgetByUserAndCategory" resultMap="FinBudgetResult">
        <include refid="selectFinBudgetVo"/>
        where b.user_id = #{userId}
        and b.category_id = #{categoryId}
        and b.year = #{year}
        and b.month = #{month}
        and b.del_flag = '0'
    </select>

    <!-- 检查预算是否存在 -->
    <select id="checkBudgetUnique" parameterType="FinBudget" resultMap="FinBudgetResult">
        <include refid="selectFinBudgetVo"/>
        where b.user_id = #{userId}
        <if test="categoryId != null">and b.category_id = #{categoryId}</if>
        <if test="categoryId == null">and b.category_id is null</if>
        and b.year = #{year}
        and b.month = #{month}
        and b.del_flag = '0'
        <if test="id != null">and b.id != #{id}</if>
        limit 1
    </select>

    <!-- 查询用户指定月份所有预算 -->
    <select id="selectFinBudgetByMonth" resultMap="FinBudgetResult">
        <include refid="selectFinBudgetVo"/>
        where b.user_id = #{userId}
        and b.year = #{year}
        and b.month = #{month}
        and b.del_flag = '0'
        order by b.category_id
    </select>

    <!-- 查询超出预警阈值的预算 -->
    <select id="selectWarningBudgets" resultMap="FinBudgetResult">
        <include refid="selectFinBudgetVo"/>
        where b.user_id = #{userId}
        and b.year = #{year}
        and b.month = #{month}
        and b.warned = 1
        and b.del_flag = '0'
    </select>

    <!-- 新增预算 -->
    <insert id="insertFinBudget" parameterType="FinBudget" useGeneratedKeys="true" keyProperty="id">
        <!-- 确保year和month要么都有值，要么都为空 -->
        <if test="(year != null and month != null) or (year == null and month == null)">
            insert into fin_budgets (
            user_id,
            category_id,
            year,
            month,
            amount,
            warning_threshold,
            warned,
            create_by,
            create_time,
            remark
            ) values (
            #{userId},
            #{categoryId},
            #{year,jdbcType=INTEGER},
            #{month,jdbcType=INTEGER},
            #{amount},
            #{warningThreshold},
            #{warned},
            #{createBy},
            sysdate(),
            #{remark}
            )
        </if>
    </insert>

    <!-- 修改预算 -->
    <update id="updateFinBudget" parameterType="FinBudget">
        update fin_budgets
        <set>
            <if test="amount != null">amount = #{amount},</if>
            <if test="warningThreshold != null">warning_threshold = #{warningThreshold},</if>
            <if test="warned != null">warned = #{warned},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate(),
            <if test="remark != null and remark != ''">remark = #{remark},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 更新预算预警状态 -->
    <update id="updateBudgetWarningStatus" parameterType="FinBudget">
        update fin_budgets
        set warned = #{warned},
        update_time = sysdate()
        where id = #{id}
    </update>

    <!-- 标记已警告 -->
    <update id="markAsWarned" parameterType="Long">
        update fin_budgets
        set warned = 1,
        update_time = sysdate()
        where id = #{id}
    </update>

    <!-- 删除预算 -->
    <update id="deleteFinBudgetById" parameterType="Long">
        update fin_budgets set del_flag = '2' where id = #{id}
    </update>

    <!-- 批量删除预算 -->
    <update id="deleteFinBudgetByIds" parameterType="String">
        update fin_budgets set del_flag = '2' where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
